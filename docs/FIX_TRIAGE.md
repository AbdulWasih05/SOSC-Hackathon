# Fix-pass triage (started 2026-07-12, ~T-10h)

Severity: S1 demo-breaking, S2 wrong-on-screen, S3 cramped/confusing UI (logged only this pass), S4 cosmetic/cleanup.
Box = time budget. Status: OPEN / FIXED / PARKED / NEEDS-INFO.

| ID | Symptom | Severity | Suspected/actual file(s) | Box | Status |
|----|---------|----------|--------------------------|-----|--------|
| T1 | Protected areas not highlighted on map. Root cause: zones/patrols fetched once at map load with silent catch; if dashboard loads while engine is down (or engine restarts before first load), zones never appear even though the websocket auto-reconnects. | S2 | dashboard/src/map/MapView.jsx | 30m | FIXED: fetch now retries every 2s until success; layers insert beneath vessel dots. Verified by headless screenshot with engine up (zones, land, patrols all render). |
| T2 | Alerts-tab routing rule. Iterated many times this pass, in order: (1) DARK_EVENT-or-CRITICAL, (2) reverted to DARK_EVENT-only per human decision (matched teammate's commit 14fa510), (3) human hit CRITICAL-in-Logs again live (screenshot: "BOARD RPA11" CRITICAL in Logs), asked for a real fix, (4) simplified to severity===CRITICAL only (any kind), (5) human then flagged SPOOF/TRAWL showing in Alerts as wrong ("they should be in logs"), and with BOARDING_RECOMMENDED removed (see T12) and ZONE staying at weight 90 ("fine as its at 90"), human specified the final rule directly: only ZONE_VIOLATION, at HIGH or CRITICAL. | S2 | dashboard/src/theme.js, panels/AlertFeed.jsx, panels/FeatureCards.jsx, App.jsx | 30m | FIXED (final, human-specified rule): isAlertTab(a) = a.kind === 'ZONE_VIOLATION' && (severity === 'CRITICAL' \|\| severity === 'HIGH'). Since ZONE_VIOLATION severity is always HIGH or CRITICAL by construction (never MEDIUM/LOW), this is effectively "all zone violations, any kind excluded" — DARK_EVENT, SPOOF_TELEPORT, fishing patterns, and ILLEGAL_FISHING_SUSPECTED all route to Logs now, unconditionally. Empty-state copy "No zone violations detected", card sub-label "zone violations". Verified: vite build clean; go test ./... green; live 30s sample showed a SPOOF_TELEPORT:HIGH alert that correctly excludes under the new rule. CAVEAT still open: DARK_EVENT (the pitch's core money-shot alert) is now in Logs, not Alerts, under every rule iteration since (4) — human has been actively steering this and has not flagged it as wrong, but it's worth a final confirmation before the demo since it's a departure from the original pitch narrative. |
| T12 | BOARDING_RECOMMENDED (the risk sweeper's CRITICAL tier-crossing escalation alert) removed completely per explicit human request, after it was found to fire redundantly alongside a plain CRITICAL ZONE_VIOLATION for the same event (zone alone scores 90, auto-CRITICAL, so one MPA entry produced two alerts). | Feature removal | engine/internal/risk/sweeper.go | 15m | SHIPPED: Tick() no longer checks for an upward crossing into CRITICAL tier (only the HIGH crossing / ILLEGAL_FISHING_SUSPECTED remains); emit()'s dead BOARDING-only intercept-building branch and its now-unused `math` import removed. alert.KindBoarding constant and dashboard KIND_COLOR/KIND_LABEL entries left in place (harmless, unreferenced — dead-code removal there logged as S4, not urgent). Verified: go build/vet/test all clean; 30s live sample fired zero BOARDING_RECOMMENDED alerts. |
| T3 | Dark-event storm on live feed: many unrelated vessels alert DARK at silence ~100s (exact threshold for the <=14 kn class at 10x). Cause: routine community-AIS coverage gaps, not vessels going dark; floods the Alerts tab and dilutes the money-shot alert. | S2 | engine/internal/check/dark.go, cmd/palkwatch/main.go | 15m | FIXED (human-directed): live feed now has a 900s (15 min) floor under the silence threshold (Sweeper.SetMinSilenceS), set only in runLive. Scenario/firehose untouched (minSilenceS=0, spec's 6x formula unchanged). Verified: 15s live sample went from a steady trickle of DARK alerts to zero; go test ./internal/check/ still green. |
| T4 | Judge question "same man on every alert": P1/P2 chips appear on every dark alert. Not a bug: intercepts are advisory solutions computed per configured patrol asset (only 2 exist on the live feed). Could confuse judges; consider labeling ("if dispatched"). | S3 | dashboard/src/panels/AlertFeed.jsx | - | LOGGED (S3 pass) |
| T5 | Every dark alert cites the same zone (voordelta-n2000). Correct behavior of nearest-zone attribution with only 2 configured protected zones, but reads as a bug on screen. Consider showing distance-to-zone in the detail line. | S3 | engine/internal/check/dark.go, AlertFeed detail line | - | LOGGED (S3 pass) |
| T6 | Severity contract drift: CLAUDE.md frozen contract says severity is "HIGH \| CRITICAL" but engine now emits MEDIUM (fishing) and defines LOW; dashboard routing depends on MEDIUM. Docs/contract need human-approved update. | S4 | CLAUDE.md, engine/internal/alert/schema.go | 10m | OPEN (needs human approval, user deferred) |
| T7 | gofmt -l flags most engine files due to CRLF line endings from Windows checkout (no semantic formatting issues). Normalize with .gitattributes or gofmt sweep in a quiet moment; big-diff risk if done mid-pass. | S4 | engine/** | 15m | OPEN |
| T8 | MPA fill opacity 0.14 washes out on projector; EEZ 0.05 nearly invisible; legend panel can cover an MPA at 1366x768. | S3 | dashboard/src/map/MapView.jsx, styles | 10m | FIXED (one-off, raised directly by human as "can't see the protected zone highlighted"): fill-opacity mpa 0.14->0.32, eez 0.05->0.10; line-width mpa 1.5->2.5, eez 1->1.5; line-opacity 0.7->0.9. Verified by screenshot: MPA now solid pink with bold red border, clearly distinct from EEZ and water. Legend-over-zone overlap at 1366x768 still open, minor. |
| T11 | Fullscreen map toggle requested (not in original scope; human explicitly approved as a one-off exception ahead of the S3 freeze). | Feature (one-off) | dashboard/src/map/MapView.jsx, styles.css | 15m | SHIPPED: maplibregl.FullscreenControl added bottom-left, targets the map-container parent (not just the canvas) so the legend and connection notice stay visible in fullscreen; added explicit :fullscreen CSS sizing since the container's position:relative would otherwise fight the browser's default fullscreen layout. Verified: control renders (screenshot); Fullscreen API itself requires a user gesture so it could not be exercised headlessly, needs a manual click-test before demo. |
| T9 | Latency histogram / HUD units not yet audited against Phase 2 checklist (us vs ms labels, p99 >= p50). | S2 (audit) | dashboard/src/panels/Latency.jsx, engine/internal/alert/stats.go | 45m | OPEN (Phase 2 sweep) |
| T10 | Phase 2 calculation audit not yet run: projection radians, circular stddev in fishing detector, knots/m/s and nm/m conversions, ts_ms vs s in decay and r(t), intercept ETA sanity test, score cap and tier boundary consistency. | S2 (audit) | engine/internal/geo/, risk/, check/fishing.go | 2h | OPEN (Phase 2 sweep) |

## Notes

- Judge critique feedback and team bug list NOT yet received; table only holds self-observed items plus the user-reported T1/T2. Intake pending.
- Pre-fix-pass tag not created (user deferred commits). Escape pod is origin/master f03b4ce plus this working tree; commit early when the user is ready.
- Quick bench baseline not yet recorded this pass (BENCH_LOG.md last entry predates the pulled fishing-gate change); run before the next engine-touching fix.
